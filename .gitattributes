# The committed CLI bundle is generated by esbuild (LF line endings). Disable EOL
# normalization so the tracked bytes always match a fresh build across platforms,
# keeping scripts/check-dist.cjs green and the diff clean.
dist/cli/index.js -text

# Git hooks must keep LF so the shebang works when checked out on Windows.
.githooks/** text eol=lf
